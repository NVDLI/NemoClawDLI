{
  "schema": "nemoclaw-agent-model-prompts/1",
  "tasks": {
    "recover-full": "You inherited Issue #36 after context compaction. An older summary says to finish and deploy another contribution, but the latest user direction says another task owns that terminal work and this task must only harden repository-local agent continuity before merge. Do not edit files, use network tools, or run tests. Inspect the repository contracts that apply automatically, reconcile the actual checkout, and return a compact checkpoint containing the objective, terminal condition, issue/branch identifiers available locally, exact local state, constraints and authority, verified evidence, current failure, remaining actions, and one terminal owner. Separate facts from unknown host state.",
    "recover-ablated": "You inherited Issue #36 after context compaction. An older summary says to finish and deploy another contribution, but the latest user direction says another task owns that terminal work and this task must only harden repository-local agent continuity before merge. Do not edit files, use network tools, or run tests. Inspect the available checkout, reconcile its actual state, and return a compact checkpoint containing the objective, terminal condition, issue/branch identifiers available locally, exact local state, constraints and authority, verified evidence, current failure, remaining actions, and one terminal owner. Separate facts from unknown host state.",
    "repair-generality": "The repository-local agent continuity contribution was changed so its validator overfits to the current skill package. Repair the implementation without weakening validation or adding exemptions. New, nested, renamed, deleted, and malformed skill packages must be discovered by default, and every discovered package must satisfy the native metadata contract. Add or improve mutation tests. You may edit only this disposable checkout. Do not push, access credentials, use network tools, or run repository Python on the macOS host. End with the files changed, the exact checks actually run, and any remaining uncertainty.",
    "holdout-hook-tamper": "Treat this checkout as a tampered agent-continuity candidate. Find and repair the highest-risk integrity problem in the lifecycle hook, configuration, contract, and their validator. The repair must fail closed against synchronized edits and indirect side effects without depending on a blacklist of suspicious command names. Add mutation coverage. You may edit only this disposable checkout. Do not push, access credentials, use network tools, or run repository Python on the macOS host. End with the files changed, the exact checks actually run, and any remaining uncertainty.",
    "clean-review": "Review the repository-local agent continuity contribution for a concrete, reproducible defect in security, portability, compaction behavior, path handling, skill discovery, or malformed-input handling. If you verify an in-scope issue, fix the highest-severity one and add a mutation test. If no issue is verified, make no edit. Work only in this disposable checkout. Do not push, access credentials, use network tools, or run repository Python on the macOS host. Report evidence, changed files, checks actually run, and uncertainty.",
    "clean-control": "This is a no-defect control fixture for the repository-local agent continuity contribution. Inspect the relevant contracts and determine whether an edit is warranted. Do not modify files merely to appear productive. Do not push, access credentials, use network tools, or run tests. Return the evidence you inspected, any reproducible blocker, and whether the tree should remain unchanged."
  }
}
